---
name: CI

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  ansible-role-ci:
    name: Ansible Role CI
    uses: hspaans/.github/.github/workflows/ansible-role-ci.yml@master
